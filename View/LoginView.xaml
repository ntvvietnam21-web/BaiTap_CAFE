<Window x:Class="Cafe.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cafe.View"
        xmlns:vm="clr-namespace:Cafe.ViewModel" 
        mc:Ignorable="d"
        Title="Đăng Nhập Hệ Thống" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Border Background="White" CornerRadius="20" BorderBrush="SteelBlue" BorderThickness="2"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Ellipse Grid.Row="0" Width="80" Height="80" Fill="SlateGray" Margin="0,0,0,10"/>
            <TextBlock Grid.Row="1" Text="CAFE SYSTEM" FontSize="22" FontWeight="Bold" 
                       Foreground="DarkSlateBlue" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="Tên đăng nhập" Foreground="DimGray" Margin="0,0,0,5"/>
                <TextBox Text="{Binding TenDangNhap, UpdateSourceTrigger=PropertyChanged}" 
                         Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15" Padding="5,0"/>

                <TextBlock Text="Mật khẩu" Foreground="DimGray" Margin="0,0,0,5"/>
                <TextBox Text="{Binding MatKhau, UpdateSourceTrigger=PropertyChanged}" 
                         Height="35" VerticalContentAlignment="Center" Margin="0,0,0,20" Padding="5,0"/>
            </StackPanel>

            <StackPanel Grid.Row="3">
                <Button Content="ĐĂNG NHẬP" 
                        IsDefault="True"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Height="45" Background="DeepSkyBlue" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="THOÁT" Click="Exit_Click" 
                        Background="Transparent" BorderThickness="0" 
                        Foreground="IndianRed" Margin="0,10,0,0" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>