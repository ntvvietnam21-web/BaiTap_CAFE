<UserControl x:Class="Cafe.View.NhapHangUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Cafe.ViewModel"
             Background="WhiteSmoke"
             d:DesignHeight="700" d:DesignWidth="1100"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:NhapHangViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="15" Margin="0,0,10,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="1"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="CHỌN SẢN PHẨM NHẬP KHO" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                <ListBox ItemsSource="{Binding ListSanPham}" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.ChonMonCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" 
                                    Width="130" Height="90" Margin="5" Background="#ECF0F1" BorderThickness="1" BorderBrush="LightGray" Cursor="Hand">
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TenMon}" FontWeight="Bold" TextWrapping="Wrap" TextAlignment="Center" Foreground="#34495E"/>
                                    <TextBlock Text="{Binding MaMon}" Foreground="Gray" TextAlignment="Center" Margin="0,5,0,0" FontSize="11"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="15" Margin="10,0,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="CHI TIẾT PHIẾU NHẬP" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding ListGioHang}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" BorderThickness="0" RowHeight="35">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên Món" Binding="{Binding SanPham.TenMon}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Giá Nhập" Binding="{Binding DonGiaItem, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                        <DataGridTextColumn Header="SL" Binding="{Binding SoLuong, UpdateSourceTrigger=PropertyChanged}" Width="50"/>
                        <DataGridTextColumn Header="Thành Tiền" Binding="{Binding ThanhTien, StringFormat={}{0:N0}}" IsReadOnly="True" Width="90"/>

                        <DataGridTemplateColumn Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="X" Command="{Binding DataContext.XoaMonCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#E74C3C" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <TextBlock Text="Nhà cung cấp:" Foreground="Gray" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding ListNhaCungCap}" SelectedItem="{Binding SelectedNCC}" DisplayMemberPath="TenNCC" Height="35" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

                    <Border Background="#ECF0F1" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="TỔNG TIỀN NHẬP: " FontSize="18" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TongTienHD, StringFormat={}{0:N0} VNĐ}" FontSize="22" FontWeight="Bold" Foreground="#E74C3C" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Border>

                    <Button Content="TẠO PHIẾU NHẬP" Command="{Binding ThanhToanCommand}" Height="50" Background="#3498DB" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>